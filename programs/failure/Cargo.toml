[package]
name = "analog-failure-program"
version = "1.8.2"
description = "Analog failure program"
authors = ["Analog Maintainers <maintainers@analog.foundation>"]
repository = "https://github.com/analog/testnet"
license = "Apache-2.0"
homepage = "https://analog.com/"
documentation = "https://docs.rs/analog-failure-program"
edition = "2018"

[dependencies]
analog-sdk = { path = "../../sdk", version = "=1.8.2" }

[dev-dependencies]
analog-runtime = { path = "../../runtime", version = "=1.8.2" }

[lib]
crate-type = ["lib", "cdylib"]
name = "analog_failure_program"


[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]
