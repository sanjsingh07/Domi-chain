{
    "repositoryUrl": "git@github.com:analog-labs/analog-web3.js.git",
    "preset": "conventionalcommits",
    "presetConfig": {
        "issueUrlFormat": "{{host}}/{{owner}}/solana/issues/{{id}}"
    }
}
